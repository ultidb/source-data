"""`WfdfLegacySource`: `core.source.Source` implementation for WFDF's older,
static-HTML "legacy" results platform (`results.wfdf.sport/<slug>/`), which
hosts historical WFDF World Championship events (starting with WUC 2024)
that predate the newer `sources.wfdf` JSON-API platform.

See `sources/wfdf_legacy/source.py` for the module-by-module breakdown.
"""
